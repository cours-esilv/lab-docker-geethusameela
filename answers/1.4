docker stop <container_id_or_name>  # Stop the existing container
docker rm <container_id_or_name>    # Remove the existing container
docker run -d -p 5000:5000 -e CURRENT_ENVIRONMENT=production --name flask-app your-image-name
